<!DOCTYPE html>
<html>
<head>
<style> 
    body{
        background-color: #DA70D6;
    }
    .box{
        background-color: white;
        width: 50vw;
        border-radius: 10px;
        margin-left: 25vw;
    }
    .content{
        margin-left: 1%;
        margin-right: 1%;
    }
    h2{
        margin-top: 0;
    }
    #customers {
    font-family: Arial, Helvetica, sans-serif;
    border-collapse: collapse;
    width: 100%;
    }

    #customers td, #customers th {
    border: 1px solid #ddd;
    padding: 8px;
    }

    #customers tr:nth-child(even){background-color: #eaaee8;}

    #customers tr:hover {background-color: #eaaee8;}

    #customers th {
    padding-top: 12px;
    padding-bottom: 12px;
    text-align: left;
    background-color: #DA70D6;
    color: white;
    }
</style>
</head>
<body>
<div class="box">
    <div class="content">
        <br/>
        <h2><i class="fa fa-th-list"></i> Lista de Pedidos Cadastrados</h2>
        <table id="customers">
        <tr>
            <th>ID</th>
            <th>Cliente</th>
            <th>Produto</th>
            <th>Valor (R$)</th>
        </tr>
        {{#if retorno}}
            {{#each retorno}}
            <tr>
                <td>{{id}}</td>
                <td>{{cliente}}</td>
                <td>{{produto}}</td>
                <td>{{valor}}</td>
            </tr>
            {{~/each}}
        {{else if mensagem}}
            <tr style="text-align:center">
                <td colspan="4">{{mensagem}}</td>
            </tr>
        {{/if}}
        </table>
        <br/>
        <div style="float:left">
            <a href="/"><button class="button_small cor2" style="vertical-align:middle"><span>Voltar</span></button></a> 
        </div>
        <br/>
        <br/>
        <br/>
        <br/>
    </div>
</div>
</body>
</html>